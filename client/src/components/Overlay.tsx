import React, { Fragment } from "react";

interface IProps {}

export const Overlay = (props: IProps) => {
    return (
        <Fragment>
            <div></div>
        </Fragment>
    );
};

const styles = {};

export default Overlay;
