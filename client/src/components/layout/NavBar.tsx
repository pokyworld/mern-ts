import React, { Fragment } from "react";
import { NavLink } from "react-router-dom";

export const NavBar = () => {
    return (
        <Fragment>

        </Fragment>
    )
};

export default NavBar;
